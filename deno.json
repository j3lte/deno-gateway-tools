{
  "tasks": {
    "format": "deno fmt ./src/",
    "lint": "deno lint ./src/",
    "test": "deno test --allow-sys --allow-run",
    "test:watch": "deno test --watch",
    "clean": "rm -r ./coverage",
    "localTest": "deno run --allow-sys --allow-read --allow-write --allow-run ./scripts/runTest.ts"
  },
  "fmt": {
    "options": {
      "indentWidth": 2,
      "lineWidth": 100,
      "singleQuote": false,
      "useTabs": false,
      "proseWrap": "preserve"
    },
    "exclude": [
      ".coverage/",
      "npm/",
      ".github/",
      "README.md"
    ]
  },
  "lint": {
    "rules": {
      "include": [
        "ban-untagged-todo",
        "explicit-function-return-type"
      ]
    },
    "exclude": [
      "npm/"
    ]
  },
  "test": {
    "exclude": [
      "npm/",
      ".coverage/"
    ]
  }
}
